{
  "name": "candle-store",
  "version": "1.0.0",
  "description": "Terrible Candles",
  "main": "backend/app.js",
  "scripts": {
	"client": "cd client/ && npm start",
	"server": "cd backend/ && nodemon",
	"dev": "npm run client && cd ../backend && npm run server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christian Close",
  "license": "ISC"
}
